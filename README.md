# Launchpad Platform
A working prototype of the Launchpad Platform.

## Features
* User can input an idea and receive a basic validation report
* User can select from a list of pre-built templates for their idea
* User can view a dashboard with key metrics and insights

## Usage
Run the `main` function in `src/launchpad.py` to test the platform.
